# frozen_string_literal: true

source "https://rubygems.org"

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

# gem install sqlite3 --platform=ruby -- --with-sqlite3-dir=C:/distr/sqlite --with-sqlite3-include=C:/distr/sqlite  --with-sqlite3-lib=C:/distr/sqlite
# gem 'sqlite3', '~> 1.3', '>= 1.3.13'
# gem 'string_to_ipa'
gem 'iconv'
gem 'pry'
